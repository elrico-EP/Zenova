// This project uses localStorage for data persistence instead of Firebase.
// This file provides dummy exports to prevent breaking other parts of the application
// that may have had unused imports from here.

export const db = null;
export const auth = null;
