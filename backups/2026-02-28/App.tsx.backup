// BACKUP: 2026-02-28 - App.tsx
// Estados actuales:
// - applyManualOverrides: Aplica cambios manuales sobre horario base
// - currentSchedule: baseOverrides + STRASBOURG + manualOverrides (en ese orden)
// - Problema: Los cambios manuales desaparecen ~1-3 segundos después
//   - El polling trae datos viejos y los sobrescribe
//   - El grace period (5s) puede ser insuficiente si el polling falla
// 
// Línea crítica 305-308:
// const currentSchedule = useMemo(() => {
//   const withStrasbourg = applyManualOverrides(autoCurrentSchedule, baseOverrides);
//   return applyManualOverrides(withStrasbourg, manualOverrides);
// }, [autoCurrentSchedule, manualOverrides, baseOverrides, applyManualOverrides]);
//
// El handleManualChange (línea ~460) guarda a Supabase y dispatch updateDataWithUndo

// COPIA COMPLETA EN OTRO ARCHIVO SI SE NECESITA RESTAURAR
